@{
    ViewBag.ComputerName = Environment.MachineName;
    
 }
<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BootCamp Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="~/css/styleprivate.css" rel="stylesheet" />
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo">
            <i class="fas fa-graduation-cap"></i>
            <div class="logo-text"><span style="color:#3c3">B</span>ootCamp</div>
        </div>
        <div class="nav-items">
            <div class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Private" ? "active" : "")" data-action="Home">
                <a asp-controller="Home" asp-action="Private" class="nav-link">
                    <i class="fas fa-home"></i>
                    <span>Home</span>
                </a>
            </div>

            <div class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Employee" ? "active" : "")" data-action="Employee">
                <a asp-controller="Home" asp-action="Employee" class="nav-link">
                    <i class="fas fa-users"></i>
                    <span>Employees</span>
                </a>
            </div>

            <div class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Product" ? "active" : "")" data-action="Employee">
                <a asp-controller="Home" asp-action="Product" class="nav-link">
                    <i class="fas fa-box"></i>
                    <span>Products</span>
                </a>
            </div>

            <div class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Category" ? "active" : "")" data-action="Employee">
                <a asp-controller="Home" asp-action="Category" class="nav-link">
                    <i class="fas fa-list"></i>
                    <span>Categories</span>
                </a>
            </div>

            <div class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Index" ? "active" : "")" data-action="Employee">
                <a asp-controller="Home" asp-action="Index" class="nav-link">
                    <i class="fas fa-cog"></i>
                    <span>Setting</span>
                </a>
            </div>
        </div>
        <!-- Sidebar Image for whatsapp-->
        <div class="sidebar-image">
            <a href="@($"https://wa.me/")" class="text-muted small text-decoration-none" title="WhatsApp"
               target="_blank">
                <img src="~/img/Whatsapp.gif" alt="BootCamp Team">
            </a>
        </div>
    </div>

    <!-- Header -->
    <div class="header">
        <div class="greeting" data-computer-name="@ViewBag.ComputerName">
            <h1 style="color:white;">Hello, @ViewBag.ComputerName</h1>
            <p style="color:#3c3;">Welcome to your BootCamp</p>
        </div>
        <div class="header-right">
            <div class="search-box">
                <i class="fas fa-search"></i>
                <input type="text" placeholder="Search...">
            </div>
            <div class="user-profile">
                <div class="avatar">MA</div>
            </div>
        </div>
    </div>
    @RenderBody()
   

    <!-- Footer -->
    <div class="footer">
        &copy; @DateTime.Now.Year - BootCamp.<span style="color:#3c3">Team</span>
    </div>

    <script src="~/js/script.js"></script>

    <!-- Script for carousel image -->
    <script>
        let currentSlide = 0;
        const carousel = document.getElementById('carousel');
        const items = document.querySelectorAll('.carousel-item');

        function moveCarousel(direction) {
            currentSlide += direction;

            if (currentSlide < 0) {
                currentSlide = items.length - 1;
            } else if (currentSlide >= items.length) {
                currentSlide = 0;
            }

            carousel.style.transform = `translateX(-${currentSlide * 100}%)`;
        }

        // Auto carousel every 5 seconds
        setInterval(() => {
            moveCarousel(1);
        }, 5000);
    </script>
    
</body>
</html>